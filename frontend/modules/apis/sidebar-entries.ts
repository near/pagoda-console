import type { SidebarEntry } from '@/shared/utils/types';

const entries: SidebarEntry[] = [
  { display: 'APIs', route: `/apis`, routeMatchPattern: '/apis', icon: 'code', badgeText: 'Beta' },
];

export default entries;
