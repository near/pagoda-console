import type { SidebarEntry } from '@/shared/utils/types';

const entries: SidebarEntry[] = [{ display: 'Alerts', route: `/alerts`, routeMatchPattern: '/alerts', icon: 'bell' }];

export default entries;
