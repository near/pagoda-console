import { styled } from '@/styles/stitches';

export const Header = styled('header', {
  flexShrink: 0,
  padding: 'var(--space-m)',
  position: 'sticky',
  top: 0,
  background: 'var(--color-surface-3)',
  zIndex: 500,
});
